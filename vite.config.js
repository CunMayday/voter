// v1.0: Initial configuration for Vite React project with Tailwind support.
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
});
